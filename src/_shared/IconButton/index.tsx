interface IProps{
  icon: string;
  disabled: string;
  backgroundColor?: string;
  activeColor?: string;
  hoverColor?: string;
  disabledColor?: string;
}

export const IconButton = (props: IProps) => {

}